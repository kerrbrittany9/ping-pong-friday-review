// var  = parseInt($("input#number").val());
//
//     for (var index = countByNumber; index < countToNumber; index += countByNumber) {
//         countedArray.push(index + ",");
//     }
$(document).ready(function() {
  $("submitButton").click(function(event) {
    event.preventDefault();
  });
});
